#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
const char *message = "\"Programming is like building a multilingual puzzle";

fputs(message);
fputs(stderr)

return (0);
}

